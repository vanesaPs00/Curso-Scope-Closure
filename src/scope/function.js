function greeting(){
    let useName='Vanesa';
    console.log(useName);
    if (useName==='Vanesa'){

        console.log(`Hello ${useName}!`);
    }
}
greeting();
console.log(userName);// no esta asignada como global por eso no se muestra